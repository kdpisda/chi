# 2026-09-26 draft J (correctness/output, unlinked)

Status: PUBLISHED 2026-09-26 as https://x.com/kdpisda/status/2103751582248816879 (verified author and text). Do not reuse.

```
I fed chi a scripted O(n) prefix sum with an off-by-one: each slot holds the sum before it. Timed by hand: 0.07ms vs a 17ms baseline.

chi's ledger row for it:

  correct 0
  seeds_passed []
  score null

Seed 11 was off by 99.9, so chi never timed it. Wrong code gets no score.
```

X-weighted length: 278/280 (no URL).

## Why this angle
No chi/ code changed since 09-24 (upstream added only a blog post and newsletter
bookkeeping). Unused angles.md entries are heterogeneous (unverified here),
postmortem (generic), numbers (unverified, avoid) and naming (thin). Added a new
angle, `correctness`, from a real local run instead.

## Source run (2026-09-26, $0, no key)
Scripted adapter, problems/optimize_function, two canned candidates:
1. O(n) loop that appends the running total before adding x (exclusive prefix
   sums, off by one).
2. itertools.accumulate.

`chi run` result: baseline 17.39ms, champion 0.0538ms (accumulate), cost 0.
`chi ledger` row for candidate 1: correct 0, seeds_passed_json "[]",
score_value null, noise_std null. check.py on seed 11: max_abs_error 99.89,
exit 1. chi/eval/runner.py:91-112 stops at the first failing seed and only
benchmarks when correct.
Timed by hand with the pack's bench.py: 0.081, 0.071, 0.070ms (so "0.07ms").

## Checks
Shape `output` (last five: build-log, postmortem, mechanism, myth, take).
Unlinked: 09-25 was linked, so 09-26 and 09-27 carry no link.
Says "scripted". No em dash, emoji, hashtag, question opener, banned phrase,
competitor name.
