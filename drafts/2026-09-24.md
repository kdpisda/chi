# 2026-09-24 draft H (holdout / myth, unlinked)

Status: PUBLISHED 2026-09-24 13:10 IST as https://x.com/kdpisda/status/2103026665052156275 (verified author and exact text). Do not reuse.

## Text (270 chars, no URL)

```
A better benchmark score is not faster code. I scripted a candidate with a fast path only when len(xs) == 4000, the benchmark's exact input. chi scored it 99.7% faster.

On 900, 3300 and 9000 elements it gained nothing: -4.1%.

chi's new holdout gate caught it: overfit.
```

## Why this angle and shape

- New angle `holdout` (added to angles.md today): PR #2 (held-out score gate,
  `cf90ff8`) merged to main 2026-09-24 09:26 IST. Fresh, verifiable, unused.
- Shape `myth`: last five originals were take, output, build-log, postmortem,
  mechanism. Unlinked: 09-22 linked, 09-23 unlinked, cadence says unlinked.

## Fact check (run 2026-09-24 ~13:15 IST, `chi run examples/holdout.yaml`, no key, $0)

- Candidate is scripted (`examples/holdout_overfit.json`): `if len(xs) == 4000:
  return list(itertools.accumulate(xs))`, else the O(n^2) baseline. Post says
  "I scripted a candidate".
- bench.py times one fixed 4000-element input. Run: baseline 17.098 ms,
  champion 0.0526 ms, claimed_gain_pct 99.69 -> "99.7% faster".
- holdout_bench.py `SIZES = (900, 3300, 9000)`, different seed, excluded from
  agent workdirs (`holdout.files`, `chi/orchestrator/loop.py:52`).
  holdout_gain_pct -4.11 -> "-4.1%", verdict `overfit`.
- -4.1% is within the pack's own 5% max_regression noise band, so the post says
  "gained nothing", not "got slower". The held-out number drifts run to run
  (commit notes saw -1.2% and +0.4%); the verdict is the stable part.
- Control: examples/holdout_honest.json (plain accumulate) same day: +99.7%
  claimed, +99.9% held out, verdict generalizes.
