# Drafts 2026-09-16

Status: **A POSTED at 13:08 IST.** No self-reply, and no new draft. C in [2026-09-14.md](2026-09-14.md) is still queued for 2026-09-17.

- 13:04 IST run (07:34 UTC): posted A (noiseguard / myth).
  - Post: https://x.com/kdpisda/status/2100127208778146175 (07:38:39 UTC). Author @kdpisda, rendered text an exact 278-character match, verified on the live status page.
  - **No self-reply, deliberately.** The task file now caps each run at one external write and says not to attempt a self-reply in the same run. The fallback it gives is to fold the link into the body or omit it. A is 278/280 and X counts a URL as 23, so the body has no room. The link was dropped for this post.
    - Link cadence is still fine: of the last three originals, only the 2026-09-13 watchdog post is linked (B and A are both unlinked). C can carry the link.
    - `getchi.dev` has now gone out with one post out of three, not with A. The draft's `getchi.dev` self-reply in 2026-09-14.md was never used.
  - Browser: both local Chrome extensions were connected, and **the display labels have swapped.** The pinned ID `52ac8047-0796-46fb-bc92-85c7c621a16a` now shows as "Browser 2"; `9572eef7…` now shows as "Browser 1". Selected by ID, not by label. The task file pins the ID, so the label drift is cosmetic, but don't ever select by name.
  - Preflight: logged in as @kdpisda. `posts.jsonl` had no `type:"post"` line dated 2026-09-16 IST, and the profile's three posts today (Django MAILERS, the Caddyfile/TestGuru one, the blog-routine one) are all from other routines, not chi. So the daily original was unused and nothing had been posted by hand.
  - Re-audit of A against the working tree:
    - 636/652/686µs, same kernel, same code, across runs: still `chi/eval/noise.py:3`.
    - "ranked submission" is chi's own term for the authoritative tier: `chi/eval/popcorn.py:5`, "Submit = authoritative tier: a ranked leaderboard submission". A sub-noise local win burning one and regressing the public score is failure mode 6, `docs/superpowers/specs/2026-07-25-chi-v1-design.md:31`.
    - "re-run 3x, median has to beat the champion": `NoiseGuard` defaults to `n=3` (`chi/eval/noise.py:28`) and both construction sites pass `n=3` explicitly (`chi/session/director_runner.py:76,79`). The median test is `noise.py:44-48`.
    - 278/280 characters. No banned phrase, em dash, en dash, emoji, or hashtag. The only non-ASCII character is `µ` (MICRO SIGN), which typed and rendered correctly.
    - The caveat held: A says nothing about races or correctness. NoiseGuard doesn't catch those.
  - Composer read-back before posting: 5 Draft.js blocks, exact match, 278 characters. Note for next time: reading blocks with `innerText` adds a phantom `\n` per empty block (it reports 280). Use `textContent` per block and join with `\n`.
  - The toast was gone after a 5s wait, so the status ID came from the profile timeline instead. Note: `javascript_tool` returns any `href` as `[BLOCKED: Base64 encoded data]`, and `Number(id)` silently loses precision on a 19-digit snowflake. Extract the digits as a string and return them chunked (`id.match(/.{1,4}/g)`).
  - Tab cleanup: the task-owned tab was the one `tabs_context_mcp` opened for the new group. Closed at the end, which auto-removed the group. Nothing was left open this time.
  - `git` on this machine is blocked by the Xcode license prompt (`/usr/bin/git` shims to `/Applications/Xcode.app`). Worked around with `DEVELOPER_DIR=/Library/Developer/CommandLineTools`. A one-time `sudo xcodebuild -license` would fix it properly, and that needs the user.

## Replies to service

None. An original went out this run, and the task file only allows service replies when no original is published or attempted. Nothing was checked, so chi-post notifications are unread since the 2026-09-15 13:04 run.

## Timeline check

- Room: nothing somber dominates, so the skip rule doesn't apply. Explore's news tab led with a Delhi Police officer suspended over Instagram reels during the BRICS summit (4.3K posts), a Bengaluru footpath fixed by a resident (10K), Nithin Kamath on UPI MDR costs for brokers, a SpiceJet sit-in over 24-hour Dubai delays (390), and sewage polluting the Koraiyar river (4.4K). Trends are iOS 26, Liquid Glass, Indian markets, and politics. No outage, layoff wave, death, or disaster. A touches none of it.
- A's own territory is live today, which is the best fit it has had:
  - @dogacel0's "auto-gpu-kernel" v1.0, a fully autonomous kernel-generation loop (2026-09-15 04:42 UTC): 17 replies, 552 likes, 29.8K views. That is chi's exact problem domain.
  - A reply under it (@GlitchGazer20, 45 views): "34.93x from an agent loop is wild. I've spent way too many hours watching models celebrate random jitter as a win". That is A's thesis, said by someone else, unprompted.
  - @bnjmn_marie on quantized Qwen3.8 27B, "Same Score, 42 Different Outcomes" (23.3K views), and a reply arguing 32/113 flipped outcomes is a deployment metric, not benchmark noise.
  - A names none of them and replies to none of them. It just lands next to the conversation.
- Harness, searched `(autoresearch OR "agent harness" OR "agent loop" OR "coding agents") min_faves:50 since:2026-09-15` (top): still live but no longer dominated by one thread. @hwchase17 on agent memory (93 replies, 33.2K views), @sivalabs on harness engineering not making agents write code your way (691 likes, 43K views), @omarsar0 on keeping several harnesses in sync (8.9K views), @svpino on the Agents CLI (12.9K views). The @saranormous thread has fallen out of the top results.
- The Amazon Science piece, "Why don't machine learning research agents overfit", is still circulating a week on (a Japanese-language summary at 02:36 UTC today, the HN bot at 93 views). That is [kdpisda/chi#2](https://github.com/kdpisda/chi/pull/2) territory, the held-out score gate. Still unusable until #2 merges.

## For the next run

- **A is posted.** Don't post another original on 2026-09-16 IST.
- On 2026-09-17, re-audit C (ledger / take) and post it. Use the text in 2026-09-14.md as written. Reread its caveat first: chi does not require scope on dead-end entries, so never imply it does.
  - Decide C's link the same way A's was decided: one write per run means no self-reply. C's body is 265/280, so a URL at 23 characters plus a blank line does not fit either. Either drop the link or shorten the body deliberately. `getchi.dev` is shorter than the ledger.py URL but X still counts it as 23.
  - Cadence after A: of the last three originals (watchdog, B, A), one is linked. C may carry a link without breaking the 1-in-3 rule.
- After C, the queue is empty and used angles are watchdog, classify, noiseguard, ledger. Used shapes are postmortem, mechanism, myth, take. Build-log and output are free, and neither had honest material as of today:
  - #2 was still open and unmerged as of the 2026-09-15 run. Recheck; if it has merged and is on getchi.dev, it is honest `build-log` material and "a champion that overfit the benchmark" is a new angle for angles.md.
  - The website demo numbers (812 → 636µs, 55 benches, $1.71) are still unconfirmed as a real run, and the partial says "median of 5" while `NoiseGuard` defaults to n=3. Confirm before any `numbers` or `output` post quotes them.
- Notifications on chi posts have not been checked since 2026-09-15 13:04. The watchdog post, B, B's self-reply, and now A are all in scope for the next run that doesn't publish an original.
