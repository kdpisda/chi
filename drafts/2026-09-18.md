# Drafts 2026-09-18

Status: **D POSTED at 13:10 IST, unlinked.** Written this run from a real local
run, not from the queue (the queue was empty after C). No self-reply.

- 13:04 IST run (07:34 UTC): posted D (blackboard / output).
  - Post: https://x.com/kdpisda/status/2100852335555850616 (07:40:03 UTC). Author
    Kuldeep Pisda / @kdpisda, confirmed from the status page title and the
    `/kdpisda/status/` path.
  - Verification: composer read-back was an exact match against the audited
    string (6 Draft.js blocks, 252 characters, ASCII only). On the live status
    page `tweetText` matched the same string exactly, 252 characters. No link,
    so no card to account for this time.
  - Browser: selected by pinned ID `52ac8047-0796-46fb-bc92-85c7c621a16a`, which
    displays as "Browser 2" today. The label has now been "Browser 1" (09-15,
    09-17) and "Browser 2" (09-16, 09-18) on alternating days. Keep selecting by
    ID. Task-owned tab closed at the end; the group auto-removed.
  - Preflight: logged in as @kdpisda. `posts.jsonl` had no `type:"post"` dated
    2026-09-18 IST. The profile's two posts today (workflow_duration/spec at
    02:40 UTC, Tracxn funding medians at 01:37 UTC) are from other routines.
  - **Composer gotcha, new:** on `/compose/post` the first click went to the
    home-timeline composer *behind* the modal, and the typed text vanished. The
    read-back caught it (`tweetTextarea_0` was empty). Fix: click the modal's
    placeholder by coordinate, not by the `find` ref, then read back. Note also
    that `document.querySelectorAll('[data-testid^="tweetTextarea_"]')` returns
    six nodes when the modal is open (three aliases each for the modal and the
    background composer). Index 0-2 is the modal.
  - `javascript_tool` redacted `User-Name` as `[BLOCKED: Sensitive key]`. The tab
    title carried the author instead. Use the title, not the DOM node.

## Material: the zero-key demo is real, and it is runnable output

`examples/offline.yaml` is a scripted-playback fleet. `ScriptedAdapter`
(`chi/agents/scripted.py`) writes the i-th canned candidate from
`examples/demo_candidates.json` and then really evaluates it. No provider call,
no key.

Ran it twice into the scratchpad, `--runs-root` off the repo (`runs/` is
gitignored anyway):

```
run 1  baseline 17.562666966114193  champion 0.06233301246538758
run 2  baseline 17.671915993560106  champion 0.059790967497974634
both   champion_hash sha256:3b0056916372213710da17ded2b9591efc86b8f69300689222b1e3df200a144e
both   iterations 3, total_cost_usd 0, status done
```

Champion spread between the two runs is 4.25%, baseline spread 0.62%. Warm-venv
wall clock was 1.2s; the first run was 5.2s including venv creation.

Fact check on every claim in D:
- "needs no API key": there is no `.env` in the repo and no provider key in the
  environment (`env | grep -iE 'anthropic|openai|xai|api_key'` is empty). Both
  runs completed anyway. `budgets.total_usd` is `0.0` and every event row logs
  `cost_usd 0.0`, `tokens_in 0`, `tokens_out 0`.
- "scripted candidates": `adapter: scripted`, `script:
  examples/demo_candidates.json`. Said in the post so nobody reads the 282x
  baseline-to-champion delta as something an agent discovered. It was canned.
- 0.0623 / 0.0598 / `sha256:3b005691`: the two `chi run` JSON summaries above,
  rounded, hash truncated to its first 8 hex digits.
- "4% apart": 4.25%, rounded down to one significant figure.
- "chi keys experiments on the hash and not the score": the `experiments` table
  is keyed on `code_hash`, and `insert_experiment` returns `False` without
  writing when the hash already exists (`chi/store/ledger.py:29-30`). The hash
  is `code_hash(source)` over the candidate file (`chi/eval/runner.py:68`,
  `chi/orchestrator/loop.py:174`), normalized for line endings and trailing
  whitespace (`chi/eval/hashing.py`). `champion_hash` in the summary is the
  champion row's `code_hash` (`loop.py:372`).
- Not claimed: "no network". The offline.yaml header says it, and no provider is
  called, but I did not packet-capture the run. "No API key" is what was
  verified, so that is what the post says.

Audit: 252/280. ASCII only. No em dash, en dash, hashtag, emoji, banned phrase,
or question opener. Standalone, no link needed. Angle `blackboard` and shape
`output` were both unused. No rewrite was needed, so the one allowed rewrite
went unused.

## Link cadence

Unlinked, deliberately. C (09-17) carried `getchi.dev`. Of the last three
originals before D, one was linked, so linking D would have made it two of
three. After D the last three are A (no), C (yes), D (no). The next original may
carry a link again.

## Replies to service

None. An original went out, and the task only allows service replies when no
original is published or attempted. Notifications on chi posts have gone
unchecked since the 2026-09-15 13:04 run. In scope for the next run that does
not publish: watchdog (09-13), B and its self-reply (09-15), A (09-16), C
(09-17), D (09-18).

## Timeline check

- Room: nothing somber dominates, so the skip rule does not apply. Explore's news
  tab: Blinkit delivering the iPhone 18 Pro against overnight store queues (164
  posts), Shreyas Iyer inviting the Afghanistan captain to share the trophy (253),
  and a newlywed bride severely burned in a Darjeeling homestay fire (221).
  Trends are entertainment and politics. The burn story is one individual's
  tragedy well down the list, not a timeline dominated by one.
- D's territory is live. Searching `("benchmark noise" OR reproducib* OR "same
  code" OR dedup) (agent OR agents OR LLM OR eval) min_faves:20 since:2026-09-16`
  put @firesidealpha's quote of Sachin Katti on top: OpenAI had Astra optimize
  its own inference on NVIDIA's Rubin chip for "another 2x in 72 hours", after a
  "3x improvement out of the box". A self-optimizing loop reporting a multiplier
  with no word on how it was measured is the exact adjacency D wants. D names
  nobody and replies to no one.
- The generic harness search (`"agent harness" OR autoresearch OR "agent loop" OR
  "coding agents" min_faves:30 since:2026-09-17`) had no dominant thread today,
  just tool announcements and an AI-agent security series.

## Repository state

- **`origin/main` was 11 commits behind local `main` at the start of this run**,
  going back to `d8175c7` on 09-13. Every unpushed commit is a `chore(gtm)` from
  this routine, so no earlier run ever landed its push. Pushing this run's commit
  fast-forwards all of them.
- [kdpisda/chi#2](https://github.com/kdpisda/chi/pull/2) is still unmerged;
  `refs/pull/2/head` is `308eb6e` and `refs/heads/main` is `13fc358` on the
  remote. `build-log` stays blocked.
- `git` on this machine still needs `DEVELOPER_DIR=/Library/Developer/CommandLineTools`
  to dodge the Xcode license prompt. A one-time `sudo xcodebuild -license` would
  fix it and needs the user.

## For the next run

- **D is posted.** Do not post another original on 2026-09-18 IST.
- Shapes in the last five originals are now mechanism, myth, take, output, and
  postmortem rolls out of the window once one more original lands. `build-log` is
  the only never-used shape, and it is still blocked on #2 merging and reaching
  getchi.dev. Recheck the merge state first.
- If `build-log` is still blocked, `output` is now proven repeatable: the offline
  demo is free, deterministic in its candidates, and honest as long as the post
  says the candidates are scripted. `problems/noisy_bench` exists specifically to
  drive NoiseGuard end to end with `repeats: 1`, and has not been run yet. That
  is the next honest output post if one is needed.
- Unused angles: sandbox, heterogeneous, postmortem, numbers, naming.
- The website demo numbers (812 to 636µs, 55 benches, $1.71) are still
  unconfirmed as a real run, and the partial's "median of 5" still contradicts
  NoiseGuard's n=3. Do not quote them.
