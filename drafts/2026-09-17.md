# Drafts 2026-09-17

Status: **C POSTED at 13:07 IST, with a link.** No self-reply. The draft queue is now empty.

- 13:04 IST run (07:34 UTC): posted C (ledger / take), after one rewrite to fit `getchi.dev` in the body.
  - Post: https://x.com/kdpisda/status/2100489279164277193 (07:37:24 UTC). Author @kdpisda.
  - Verification: the composer read-back matched exactly (7 Draft.js blocks, 262 characters, 275 by X's count). On the live status page, the `tweetText` body matched exactly. X renders the trailing `getchi.dev` as a link card, not as body text. The card title is "getchi (chi) — an autoresearch harness CLI", captioned "From getchi.dev", and it links through t.co. The page title carries the full text plus `https://t.co/eFXCY3lZvN`.
    - Note for next time: a trailing URL that gets a card drops out of `tweetText`. Compare the body without the URL, then confirm the card separately.
  - Browser: selected by pinned ID `52ac8047-0796-46fb-bc92-85c7c621a16a`, which now shows as "Browser 1" again (it was "Browser 2" on 2026-09-16). The task-owned tab was the one `tabs_context_mcp` opened. Closing it removed the group.
  - Preflight: logged in as @kdpisda. `posts.jsonl` had no `type:"post"` dated 2026-09-17 IST. The profile's two posts today (Celery soft time limit, 03:38 UTC; Traefik SPA fallback, 04:42 UTC) come from other routines, not chi.
  - [kdpisda/chi#2](https://github.com/kdpisda/chi/pull/2) is still unmerged. `origin/main` is still `13fc358`. `git log --all --not main` shows the holdout branch with 6 commits (`cd81518` through `308eb6e`). Still not usable for build-log.

## The rewrite (one allowed, one used)

Audited text in [2026-09-14.md](2026-09-14.md) was 265/280. A URL costs 23 plus a blank line, so 290. Link cadence allowed a link: of the last three originals (watchdog, B, A), only watchdog was linked, and B and A are both unlinked. Two unlinked posts in a row made it worth carrying one.

Posted text (275 / 280 by X's count):

```
A dead-ends list stops agents retrying what failed. It can also kill the winner.

In my hand-run fleet, two rule-outs were tested in the wrong regime. Unscoped, they nearly did.

So chi won't log a dead end without evidence. Agents can challenge one.

getchi.dev
```

Edits against the audited draft, and why each keeps the fact check intact:
- "retrying failed ideas" → "retrying what failed". Same claim, spec `:18`.
- "kill the one that wins" → "kill the winner". The spec says unscoped negatives "nearly killed the winning direction" (`docs/superpowers/specs/2026-07-25-chi-v1-design.md:30`).
- "won't record a dead end without evidence, and agents can challenge one" → "won't log a dead end without evidence. Agents can challenge one." Same two claims:
  - Evidence is required on both write paths: `chi/cli.py:307-311` rejects empty `--evidence-json`, and `chi/agents/litellm_loop.py:190-191` returns `REJECTED`.
  - The challenge path is `chi challenge` (`chi/cli.py:320-330`). It flips the entry to `challenged` (`chi/store/ledger.py:176`), and the steering digest points agents at it (`chi/orchestrator/steering.py:66`).
- Still not claimed: that chi requires scope. It doesn't (`chi deadend --scope` defaults to `""`, `cli.py:305`).
- Rechecked today, all lines above unchanged. `list_negatives` defaults to `status="active"` (`ledger.py:149`), so a challenged entry does drop out of seed context and the steering digest.
- Scan: ASCII only. No em or en dash, hashtag, emoji, banned phrase, or question opener. `getchi.dev` returned 200.

Log line: appended to `posts.jsonl` with `linked:true`, `ts` 2026-09-17T07:37:24Z.

## Replies to service

None. An original went out this run, and the task only allows service replies when no original is published or attempted. Notifications on chi posts have gone unchecked since the 2026-09-15 13:04 run. Watchdog (09-13), B and its self-reply (09-15), A (09-16), and C (09-17) are all in scope for the next run that doesn't publish.

## Timeline check

- Room: nothing somber dominates, so the skip rule doesn't apply. Explore's news tab: protesters releasing stray dogs in a Maharashtra office (5.8K posts), DeepSeek V4.1 Flash for coding (321), Modi's 76th birthday (172K), Meta's Muse agent (740), Adidas cutting nearly half its Gurugram tech-hub jobs (2.3K), and #NationalUnemploymentDay trending. One layoff story and one related trend sit among routine news. It isn't a timeline dominated by layoffs, and C doesn't touch jobs.
- C's territory is live today. Searched `("dead ends" OR "dead end" OR "ruled out" OR autoresearch OR "agent harness") (agent OR agents OR LLM) since:2026-09-16`:
  - @Gummypooh89, replying under @brianmcgrath's "SDLC for agents" post, proposes a handoff packet that lists "what already failed", or else every new agent "relearns the same dead ends".
  - @braincramps' daily AI roundup includes "Agora: Git as shared memory for collective autoResearch", where every claim is a commit anyone can rerun, pitched as killing duplicate search.
  - Both treat the failed-attempts log as the fix. C is about how that log fails. It names nobody and replies to neither.

## For the next run

- **C is posted.** Don't post another original on 2026-09-17 IST.
- The queue is empty. Used angles in the last 30 days: watchdog, classify, noiseguard, ledger. Shapes in the last five originals: postmortem, mechanism, myth, take. That leaves build-log and output unused, and angles blackboard, sandbox, heterogeneous, postmortem, numbers, naming.
  - build-log: still blocked on #2 merging and reaching getchi.dev.
  - output: the website demo numbers (812 → 636µs, 55 benches, $1.71) are still unconfirmed as a real run, and the partial's "median of 5" contradicts NoiseGuard's n=3. Don't quote them.
  - All four published shapes sit inside the last-five window, and it only rolls when something new is posted. So the next original has to be build-log or output. Honest options are an `output` post built from real `chi` terminal output captured from a local run, or no original that day, servicing replies instead.
- Link cadence: C is linked. The next two originals must be unlinked to stay at 1 in 3.
