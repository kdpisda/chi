# 2026-09-25 draft I (substrate/take, linked)

Status: PUBLISHED 2026-09-25 as https://x.com/kdpisda/status/2103389070701547614 (verified author and text). Do not reuse.

```
Installed is not working. A CLI on PATH can still reject every model on your login.

A chi dogfood run was lost to that, and to a second CLI's wrong command template. Both surfaced mid-run.

Now chi providers --probe pings each CLI before a fleet starts.

getchi.dev
```

X-weighted length: 279/280 (getchi.dev counted as 23).

## Why this angle
No new feature landed since 09-24 (upstream added only blog/newsletter files).
Unused angles in angles.md are heterogeneous (fleet-mix claim, unverified here),
postmortem, numbers (unverified numbers, avoid) and naming (thin). Added a new
angle, `substrate`, from real code instead of stretching one of those.

## Fact check
- chi/providers/substrate.py docstring: a dogfood run was wasted because one
  installed agent CLI's account rejected every model and another CLI's command
  template was wrong, both discovered only mid-run.
- The probe sends each installed CLI one prompt ("Reply with exactly the single
  word: PONG") and marks it FAIL on nonzero exit or an account-error signature.
- Exposed as `chi providers --probe` (chi/cli.py:99-111; confirmed in `--help`).
- It is opt-in, run by the user before launching; the post says "pings each CLI
  before a fleet starts", not that launch runs it automatically.
- Vendor CLI names left out (no competitor names rule).

## Checks
Shape `take` (not in last five: output, build-log, postmortem, mechanism, myth).
Link allowed: last linked original 09-22, then 09-23 and 09-24 unlinked.
No em dash, emoji, hashtag, question opener, or banned phrase.
