# Drafts 2026-09-13

Status: primary approved and posted as drafted: https://x.com/kdpisda/status/2099200048928346175. Alt not used.

## New post (primary)

- angle: `watchdog`
- shape: `postmortem`
- linked: **true** (no linked post has gone out yet, so this one can carry it; the next two go unlinked)
- length: 277 / 280 (X counts the link as 23)

```
One agent in my hand-run fleet looped ~30h. Cycled three identical "optimizations", posted "final submission" 7 times, never produced a benchmark number.

It said it was done. So chi doesn't ask. 10 iterations without a new eval result and it's killed.

getchi.dev
```

Fact check:
- ~30h loop, three identical "optimizations", seven "final submission" messages, zero benchmark datapoints: failure mode 1 in `docs/superpowers/specs/2026-07-25-chi-v1-design.md`.
- 10 iterations: default `eval_recency_iters` in `chi/config.py:31`. The watchdog also nudges the agent at 5 before the kill (`chi/orchestrator/watchdog.py:81`). It makes no LLM calls (`watchdog.py:1`).
- Left out for length: a kill re-leases the task and the agent is respawned from the store.

## Alt (only if you kill the primary)

- angle: `noiseguard`
- shape: `myth`
- linked: false. With the link it doesn't fit (271 + 25 = 296), so the link goes in a self-reply.
- length: 271 / 280

```
A lower number after an agent's edit isn't a win yet. Same kernel, same code, three runs: 636, 652, 686µs.

I burned a ranked submission on a "win" smaller than that spread. Public score regressed.

Now apparent wins get re-run 3x and the median has to beat the champion.
```

Self-reply if used: `getchi.dev`

Fact check:
- 636/652/686µs, same kernel across runs: `chi/eval/noise.py:3`.
- Sub-noise local win burned an authoritative submission and regressed the public score: failure mode 6 in the v1 design spec.
- NoiseGuard defaults: n=3 re-runs, the median must clear the champion by `promote_margin_pct` 0.5 (`chi/eval/noise.py:28-48`). Runs on rounds classified IMPROVING in the director.

## Replies to service

None. `posts.jsonl` was empty (first run), the @kdpisda profile has no chi posts, and notifications held no replies or mentions (only follows, Community Notes prompts, verification notices).

## Timeline check

Skimmed `autoresearch` (latest and top) and `"coding agents" min_faves:100`. There was no live conversation worth bending toward. Today's autoresearch stream is mostly papers (world-model RL for research agents, open autoresearch on elliptic-curve point addition) plus a low-reach take that autoresearch "is a prompt". So this is a cold post. The primary answers that take on its own terms (a looping agent can run for 30h and produce nothing) without replying to anyone.

## Setup done this run

- `posts.jsonl` created empty (did not exist).
- `angles.md` created, seeded with the 10-angle bank from the task file, plus an "Added by hand" section.
- No commits. Step 7 commits `posts.jsonl`, `angles.md`, and this file after posting.

## Log line to append on approval (fill url and ts at post time)

```
{"url":"<url>","angle":"watchdog","shape":"postmortem","linked":true,"text":"<exact posted text>","ts":"<iso8601>","type":"post"}
```
